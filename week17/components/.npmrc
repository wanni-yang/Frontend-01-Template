#
# Lock NPM settings, preventing inconsistency in `package-lock.json` caused by
# `npm install` in different environments.
#
# NOTE: This file should ONLY be kept in the source repository, and NEVER
#       should it be distributed into release packages!
#

registry=https://registry.npm.taobao.org

disturl=https://npm.taobao.org/dist
